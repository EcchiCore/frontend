import subscriptionPlan from './subscriptionPlan'
import tool from './tool'
import adRedirectSettings from './adRedirectSettings'

export const schemaTypes = [tool, subscriptionPlan, adRedirectSettings]